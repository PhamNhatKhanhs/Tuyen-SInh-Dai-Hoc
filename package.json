{
  "dependencies": {
    "@ant-design/icons": "^6.0.0",
    "antd": "^5.25.4",
    "lucide-react": "^0.513.0"
  },
  "scripts": {
    "install-all": "cd frontend-app && npm install && cd ../backend-app && npm install",
    "build": "cd frontend-app && npm run build",
    "deploy": "vercel --prod"
  }
}
